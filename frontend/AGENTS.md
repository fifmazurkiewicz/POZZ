# POZZ frontend

Next.js PWA on Vercel. **Not scaffolded yet** — Task 0 of `docs/superpowers/plans/2026-09-07-greenfield-scaffold.md`.

Product UI copy is Polish (preserve). Agent chat and new identifiers stay English.

```bash
npm install
npm run dev
npm run lint && npm test && npm run build
```

Env: `frontend/.env.example`. API pulse: `GET /api/health`. Names: `docs/technical/configuration.md`.
