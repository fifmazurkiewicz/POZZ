# Environment selection
# local -> load variables from this file via python-dotenv
# anything else (e.g., prod, staging) -> use AWS Secrets Manager
ENVIRONMENT=local

# OpenRouter API key (used when ENVIRONMENT=local)
OPENROUTER_API_KEY=sk-or-REPLACE_ME

# AWS region (used for Secrets Manager in non-local environments)
AWS_REGION=eu-central-1

# Name of the secret in AWS Secrets Manager that stores the OpenRouter key
# Accepts either a raw string value or JSON with field OPENROUTER_API_KEY
OPENROUTER_SECRET_NAME=med-sim/openrouter

# PostgreSQL connection URL
# Format examples:
# - postgresql+psycopg://user:password@localhost:5432/medsim
# - postgres://user:password@host:5432/dbname
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/medsim

# Name of the secret in AWS Secrets Manager that stores the database URL
# Accepts either a raw string value or JSON with field DATABASE_URL
POSTGRES_SECRET_NAME=med-sim/postgres