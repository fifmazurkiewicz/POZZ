# Requirements dla testów diarization i transkrypcji na żywo
# 
# Pliki:
# - test_chunking_gradio_diarization.py: Transkrypcja + diarization (Deepgram API lub whisper-diarization)
# - test_chunking_gradio.py: Transkrypcja + rozpoznawanie ról przez LLM (lekarz/pacjent)

# Podstawowe zależności
gradio>=4.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
requests>=2.31.0
soundfile>=0.12.0

# Audio streaming (opcjonalne, ale zalecane)
sounddevice>=0.4.6

# Dla test_chunking_gradio.py (rozpoznawanie ról przez LLM)
groq>=0.4.0
openai>=1.0.0

# Dla test_chunking_gradio_diarization.py - Deepgram API
# (nie wymaga dodatkowych pakietów, używa requests)

# Dla test_chunking_gradio_diarization.py - whisper-diarization (lokalne, opcjonalne)
# Wymagane tylko jeśli USE_DEEPGRAM=false:
# faster-whisper>=0.9.0
# torch>=2.0.0  # z CUDA jeśli dostępne
# ctranslate2>=3.20.0  # z CUDA jeśli dostępne
# pyannote.audio>=3.0.0
# nemo-toolkit[all]>=1.20.0

# Uwaga: whisper-diarization wymaga również zewnętrznych repozytoriów:
# - whisper-diarization: https://github.com/MahmoudAshraf97/whisper-diarization
# - ctc-forced-aligner: https://github.com/1kaiser/ctc-forced-aligner

